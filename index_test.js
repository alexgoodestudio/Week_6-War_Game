var expect = chai.expect;

describe("popOne",function(){
describe()
it(' should pop off last element of an array and push to new array')
function()
expect          equal
it("should throw an error if argument is not an array ",function (){
    expect(function(){
        popOne(array){
            return array.pop();
    }).to.throw Error('argument must be an array');
});
}
